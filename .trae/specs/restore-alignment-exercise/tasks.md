# Tasks

- [x] Task 1: 恢复背景红蓝参考线
  - [x] SubTask 1.1: 将 `.reference-grid` 的 `background-image` 中水平线改为蓝色（`rgba(60, 200, 255, 0.45)`），垂直线改为红色（`rgba(255, 60, 60, 0.45)`）。
- [x] Task 2: 恢复专业测试文案
  - [x] SubTask 2.1: 将页面提示 `<p>` 替换为 `<div>` 包裹的两步指引：“第 1 步 (测水平/垂直偏移距离)”和“第 2 步 (测旋转偏角)”。
- [x] Task 3: 去除测试区焦点光圈
  - [x] SubTask 3.1: 将 `.right-box:focus` 中的 `box-shadow` 设置为 `outline: none;` 或直接移除。
- [x] Task 4: 提交并确认修复
  - [x] SubTask 4.1: 确认页面效果，提交代码。

# Task Dependencies
- 无相互依赖，可同步修改。