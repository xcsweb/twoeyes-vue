- [x] 在 `HomeMenuLevel.vue` 成功渲染“复视检查”的新卡片。
- [x] `useFlowManager.ts` 包含并能正确处理 `diplopia` 模式的路由进退栈（跳过页面也能正常后退）。
- [x] 能够成功运行单眼复视（Monocular Diplopia），用户遮盖眼睛后，如果看到重影可以调出十字方向键（或滑块/拖拽）把重影客观标定出来。
- [x] 能够成功运行双眼复视（Binocular Diplopia），戴 3D 眼镜后红蓝点分离的现象可以通过用户主动操控按键合二为一，测得偏移值。
- [x] `TheoryLevel.vue` 里新增了关于“单双眼复视检查”的逻辑说明（屈光性复视 vs. 斜视性复视）。
- [x] 复视检查完成后的 `DiplopiaAdvice` 结果页能输出检测结果，并提供下一步的建议（就医还是训练）。